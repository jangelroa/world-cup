import React from "react";

const UserScores = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
        background: "#ddd",
      }}
    >
      <h1>User Scores</h1>
    </div>
  );
};

export default UserScores;
